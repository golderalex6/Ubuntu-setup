#Vietnamese telex,vni installation
	sudo add-apt-repository ppa:bamboo-engine/ibus-bamboo
	sudo apt-get update
	sudo apt-get install ibus ibus-bamboo --install-recommends
	ibus restart

	# Đặt ibus-bamboo làm bộ gõ mặc định

	env DCONF_PROFILE=ibus dconf write /desktop/ibus/general/preload-engines "['BambooUs', 'Bamboo']" && gsettings set org.gnome.desktop.input-sources sources "[('xkb', 'us'), ('ibus', 'Bamboo')]"

#Font Setting
	#install your NerdFont(after install,you can use all NerdFont icon,and don't have to do anything if you don't want to change your curren font)
		download the zipped font folder,unzip it on ~/.local/share/fonts (if don't have,just create it)
		run fc-cache -f -v

#Remove the execute permission for only files in folder but not the folder(for backup):
	chmod -R -x+X -- 'folder with restored backup'
